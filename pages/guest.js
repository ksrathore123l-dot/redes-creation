const menu = document.querySelector('.get-hamb'); const nav = document.querySelector('.get-header nav'); menu?.addEventListener('click', () => nav.classList.toggle('get-open')); document.querySelectorAll('nav a').forEach(a => a.addEventListener('click', () => nav.classList.remove('get-open'))); const io = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) { e.target.classList.add('get-show'); io.unobserve(e.target) } }), { threshold: .12 }); document.querySelectorAll('.get-fade').forEach(e => io.observe(e)); lucide.createIcons();